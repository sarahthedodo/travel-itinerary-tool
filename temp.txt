请帮我针对目前的 Wabi-Sabi 风格旅行行程应用进行以下 4 项具体的 UI/UX 与逻辑优化：
1. 放大 "Add Itinerary Node"（添加行程节点）页面的选择卡片
问题：目前页面上的图标和文字描述太小，导致文字字体根本看不清，只能靠 Emoji 盲猜，体验较差。
改动：
显著增大节点类型选择卡片（Grid Cards）的尺寸、Padding 与内边距，使其更加触控友好（Touch-friendly）。
增大 Emoji / Icon 的尺寸（例如 text-3xl 或 text-4xl）。
增大标题与下方描述文字的字号（标题调大至 text-base 或 text-lg，描述调大至 text-sm），并提高文字对比度，确保文字清晰易读。
保持整体 Washi-paper 柔和配色与极简高白空隙风格。
2. 支持修改“备选方案（Alternative Options）”标题
问题：添加备选方案标题后不可编辑。
改动：
将备选方案标题区域改造为可编辑状态（例如：点击标题变为输入框，或旁边提供一个优雅的编辑图标/双击直接内联编辑）。
支持用户修改备选方案的标题名称，并在修改后自动同步保存状态。
3. 全局默认语言设为中文
改动：
将应用的默认语言（i18n / 多语言设置）初始化改为 中文（zh-CN）。
只有当用户显式在设置或语言切换器中选择英文时，才切换为英文模式。
4. 密码解锁后的用户身份与 Persona 设定优化
问题：输入密码后直接进入且默认用户名为 "traveler user"，缺乏个性化。
改动：
输入密码解锁成功后，不要直接进入主界面。
自动弹出 Persona 设定弹窗（允许用户选择/修改自己的身份名称与头像/角色）。
将系统默认的用户名称从 "traveler" / "traveler user" 修改为 "momo"。
用户确认或关闭 Persona 弹窗后，再平滑过渡进入行程主界面。
请逐一更新相关组件代码，并提供完整且可直接替换的代码片段。